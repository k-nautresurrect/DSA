/*
 # is the stringinizing operator used for various depending on its role,
 when used with include it means that whatever start from # is preprocessors

 iostream is the header file which will help to convert the code direct in binaries.
*/
#include<iostream>

using namespace std;
// using namespace initialises that the scope of the program is valid till std (namespace).

int main(){
	cout << "HELLO WORLD !" << endl;
	return 0;
}